#include <iostream>
#include <vector>
using namespace std;

int busca_vaga(){

}

int main(){
    int c, n;
    
    vector<int> v(c, -1);
    while (cin >> c >> n){
        long long arrecadado = 0;
        char evento;
        int placa, tamanho_veiculo;
        cin >> evento >> placa;
        if (evento == 'C'){
            cin >> tamanho_veiculo;

            arrecadado += 10;
        } else{

        }
    }
    
    
}