#include <iostream>
using namespace std;
// antes de enviar a string, ordenar 
// dica: ver a ocorrência dos caracteres

int main(){
    string s, sa, sp;
    int q, t, k;
    int x, y;
    for (int i = 0; i < q; i++){
        cin >> t;
        if (t == 1){
            cin >> k >> s;
            sa = sa + s;
        } else if (t == 2){
            cin >> k >> s;
            sp = sp + s;
        }
        else {
                
        }
    }
}